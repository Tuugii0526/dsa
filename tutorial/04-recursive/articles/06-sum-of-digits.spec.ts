import { soft0406 } from "./06-sum-of-digits";

describe("sum of digits", () => {
  it("hi", () => {
    expect(soft0406(4, 0)).toBe(4);
    expect(soft0406(14, 0)).toBe(5);
    expect(soft0406(1234, 0)).toBe(10);
  });
});
